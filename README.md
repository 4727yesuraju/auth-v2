# auth-v2
MERN Authentication System with JWT, HTTP-only Cookies, OAuth &amp; MFA Production-ready authentication system using access/refresh tokens, secure cookie handling, Google OAuth, and multi-factor authentication (OTP/TOTP).
